version: 0.2

phases:
    install:
        commands:
            - npm install
    build:
        commands:
            - npm run release
            - zip -r dist.zip dist/AngSix/

artifacts:
    files:
        dist.zip
